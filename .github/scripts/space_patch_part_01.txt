    (657, 657, "const SPACE_PALETTE = ['#9E0142','#D53E4F','#F46D43','#FDAE61','#FEE08B','#FFFFBF','#E6F598','#ABDDA4','#66C2A5','#3288BD','#5E4FA2'];\n"),
    (666, 667, 'const SECONDARY_FIRE_RATE = 30; // 2 missiles per second at the fixed 60 Hz simulation rate\n'),
    (670, 682, '    FIGHTER: { health: 1, score: 10, width: 32, height: 29 },\n    WAVER: { health: 2, score: 20, width: 35, height: 24 },\n    DIVER: { health: 1, score: 15, width: 27, height: 37 },\n    SNIPER: { health: 3, score: 30, width: 30, height: 30 },\n    SAUCER: { health: 4, score: 50, width: 38, height: 22 },\n    BOMBER: { health: 5, score: 40, width: 42, height: 32 },\n    GUARDIAN: { health: 10, score: 75, width: 38, height: 38 },\n    ASSASSIN: { health: 2, score: 100, width: 22, height: 40 },\n    CARRIER: { health: 15, score: 150, width: 51, height: 40 },\n    DRONE: { health: 1, score: 5, width: 16, height: 16 },\n    SPINNER: { health: 3, score: 45, width: 34, height: 34 },\n    SHIELDER: { health: 25, score: 100, width: 43, height: 43 },\n'),
    (1422, 1426, "const ENDGAME_PROMOTIONS = [\n    { FIGHTER: 'WAVER', WAVER: 'DIVER', DIVER: 'SNIPER' },\n    { FIGHTER: 'DIVER', WAVER: 'SNIPER', DIVER: 'SPINNER', SNIPER: 'BOMBER' },\n    { FIGHTER: 'SNIPER', WAVER: 'SPINNER', DIVER: 'ASSASSIN', SNIPER: 'GUARDIAN', BOMBER: 'CARRIER' }\n];\nconst buildEndgameLevel = (base, tier) => base.map((wave, waveIndex) => ({\n    delay: Math.max(105, wave.delay - tier * 12),\n    enemies: wave.enemies.map((info, enemyIndex) => {\n        const promote = ENDGAME_PROMOTIONS[tier - 1] || {};\n        const shouldPromote = ((waveIndex + enemyIndex + tier) % (5 - Math.min(tier, 3))) === 0;\n        const type = shouldPromote ? (promote[info.type] || info.type) : info.type;\n        const mirror = tier % 2 === 1 && waveIndex % 2 === 1;\n        const x = mirror ? Math.max(30, Math.min(730, GAME_WIDTH - info.x - 40)) : info.x;\n        const options = { ...(info.options || {}) };\n        if (options.speedY) options.speedY += tier * 0.12;\n        return { ...info, type, x, options };\n    })\n}));\nlevelDesigns[18] = buildEndgameLevel(levelDesigns[17], 1);\nlevelDesigns[19] = buildEndgameLevel(levelDesigns[17], 2);\nlevelDesigns[20] = buildEndgameLevel(levelDesigns[17], 3);\n"),
    (1431, 1432, '        color: SPACE_PALETTE[Math.floor(Math.random() * SPACE_PALETTE.length)],\n'),
    (1448, 1450, ''),
    (1463, 1464, '        settings, currentWave: 0, waveSpawnTimer: 180, allWavesSpawned: false,\n'),
    (1561, 1562, "    (0, audioService_1.playSound)('missile');\n"),
    (1622, 1622, "            if (['ArrowLeft','ArrowRight','ArrowUp','ArrowDown','Space','AltLeft','AltRight','KeyW','KeyA','KeyS','KeyD','KeyX','KeyZ','ShiftLeft','ShiftRight','KeyB','KeyC','KeyV'].includes(e.code)) e.preventDefault();\n"),
    (1715, 1717, "            canvas.addEventListener('touchstart', handleTouchStart, { passive: false });\n            canvas.addEventListener('touchmove', handleTouchMove, { passive: false });\n"),
    (1782, 1782, "const refreshDamageState = (player) => { player.damageState = player.lives >= 3 ? 'PRISTINE' : player.lives === 2 ? 'DAMAGED' : 'CRITICAL'; };\n"),
    (1831, 1832, '        refreshDamageState(state.player);\n        if (state.player.lives <= 0) {\n'),
    (1841, 1841, '    }\n    if (state.player.fuel > 0 && state.player.fuel / state.player.maxFuel < 0.15 && state.gameTime % 120 === 0) {\n        (0, audioService_1.playSound)(\'lowFuel\');\n        state.floatingTexts.push({ id: `ft-lowfuel-${Date.now()}`, text: "LOW FUEL", x: state.player.x - 8, y: state.player.y - 14, life: 45, color: themeColorsRef.current?.damageMedium || \'#FEE08B\', size: \'normal\' });\n'),
    (1876, 1877, '            state.player.fireSecondaryCooldown = SECONDARY_FIRE_RATE;\n'),
    (1909, 1910, "                        const powerUpTypes = ['FUEL', 'AMMO', 'REPAIR', 'SHIELD', 'TRIPLE_SHOT', 'HOMING_MISSILES'];\n"),
    (1948, 1949, '                refreshDamageState(state.player);\n                if (state.player.lives <= 0) {\n'),
    (2028, 2028, '                    refreshDamageState(state.player);\n'),
    (2031, 2031, '                    refreshDamageState(state.player);\n'),
    (2042, 2043, ''),
    (2081, 2081, "            if (Math.random() < 0.22) (0, audioService_1.playSound)('enemyLaser');\n"),
    (2113, 2113, "    const spaceBg = ctx.createLinearGradient(0, 0, 0, GAME_HEIGHT);\n    spaceBg.addColorStop(0, '#0b0915'); spaceBg.addColorStop(0.52, '#111426'); spaceBg.addColorStop(1, '#090711');\n    ctx.fillStyle = spaceBg; ctx.fillRect(0, 0, GAME_WIDTH, GAME_HEIGHT);\n    const nebula = ctx.createRadialGradient(GAME_WIDTH * .22, GAME_HEIGHT * .28, 20, GAME_WIDTH * .22, GAME_HEIGHT * .28, GAME_WIDTH * .58);\n    nebula.addColorStop(0, 'rgba(94,79,162,.15)'); nebula.addColorStop(.5, 'rgba(50,136,189,.055)'); nebula.addColorStop(1, 'rgba(9,7,17,0)');\n    ctx.fillStyle = nebula; ctx.fillRect(0, 0, GAME_WIDTH, GAME_HEIGHT);\n"),
    (2146, 2147, "    s.bullets.forEach(b => { const c = b.type === 'player' ? themeColorsRef.current.bulletPlayer : themeColorsRef.current.bulletEnemy; ctx.save(); ctx.fillStyle = c; ctx.shadowColor = c; ctx.shadowBlur = b.isHoming ? 12 : 7; ctx.fillRect(b.x, b.y, b.width, b.height); if (b.isHoming) { ctx.globalAlpha = .55; ctx.fillRect(b.x - 2, b.y + b.height, b.width + 4, 7); } ctx.restore(); });\n"),
    (2155, 2171, '        if (vJoy.active) {\n            ctx.beginPath(); ctx.arc(vJoy.base.x, vJoy.base.y, 60, 0, Math.PI * 2);\n            ctx.fillStyle = "rgba(230,245,152,0.16)"; ctx.fill(\'nonzero\');\n            ctx.strokeStyle = "rgba(102,194,165,0.55)"; ctx.lineWidth = 2; ctx.stroke();\n            ctx.beginPath(); ctx.arc(vJoy.handle.x, vJoy.handle.y, 38, 0, Math.PI * 2);\n            ctx.fillStyle = "rgba(102,194,165,0.34)"; ctx.fill(\'nonzero\');\n        }\n        const controls = [\n            { x: GAME_WIDTH - 80, r: 50, fill: \'rgba(213,62,79,0.52)\', label: \'FIRE\' },\n            { x: GAME_WIDTH - 170, r: 40, fill: \'rgba(50,136,189,0.52)\', label: \'MISS\' },\n            { x: GAME_WIDTH - 250, r: 40, fill: \'rgba(158,1,66,0.52)\', label: \'BOMB\' }\n        ];\n        controls.forEach(c => { ctx.beginPath(); ctx.arc(c.x, GAME_HEIGHT - 90, c.r, 0, Math.PI * 2); ctx.fillStyle = c.fill; ctx.fill(\'nonzero\'); ctx.strokeStyle = \'rgba(255,255,191,.55)\'; ctx.lineWidth = 2; ctx.stroke(); ctx.fillStyle = \'#FFFFBF\'; ctx.font = \'bold 11px monospace\'; ctx.textAlign = \'center\'; ctx.fillText(c.label, c.x, GAME_HEIGHT - 86); });\n        ctx.textAlign = \'start\';\n'),
    (2180, 2181, '    const { upgrades, settings, setLastGameStats, addCredits, updateLifetimeStats, currentLevel, t } = (0, GameContext_1.useGameState)();\n'),
    (2198, 2198, '        comboMultiplier: 1,\n        comboCount: 0,\n'),
    (2201, 2202, ''),
    (2205, 2216, '                score: s.stats.score, stage: s.stats.stage, lives: s.player.lives,\n                fuel: s.player.fuel, maxFuel: s.player.maxFuel, ammo: s.player.ammo, maxAmmo: s.player.maxAmmo,\n                shieldTimer: s.player.shieldTimer, tripleShotTimer: s.player.tripleShotTimer,\n                atomicBombs: s.player.atomicBombs, homingMissiles: s.player.homingMissiles,\n                comboMultiplier: s.combo.multiplier, comboCount: s.combo.count,\n'),
    (2218, 2219, ''),
