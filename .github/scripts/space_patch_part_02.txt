    (2220, 2222, '        updateHUD();\n        const timer = window.setInterval(updateHUD, 100);\n        return () => window.clearInterval(timer);\n'),
    (2252, 2252, '                    updateLifetimeStats(gameState.current.stats);\n'),
    (2258, 2258, '                    updateLifetimeStats(gameState.current.stats);\n'),
    (2262, 2263, '    }, [isPaused, upgrades, getInput, setLastGameStats, onGameOver, onLevelComplete, addCredits, updateLifetimeStats]);\n'),
    (2277, 2282, '        let frameId;\n        let lastTime = performance.now();\n        let accumulator = 0;\n        const STEP_MS = 1000 / 60;\n        const tick = (now) => {\n            const elapsed = Math.min(100, Math.max(0, now - lastTime));\n            lastTime = now;\n            accumulator += elapsed;\n            let catchUpSteps = 0;\n            while (accumulator >= STEP_MS && catchUpSteps < 5) {\n                gameLoopRef.current();\n                accumulator -= STEP_MS;\n                catchUpSteps++;\n            }\n            if (catchUpSteps === 5) accumulator = 0;\n            drawLoopRef.current();\n            frameId = requestAnimationFrame(tick);\n'),
    (2283, 2293, '        frameId = requestAnimationFrame(tick);\n        return () => cancelAnimationFrame(frameId);\n'),
    (2294, 2295, '    (0, react_1.useEffect)(() => { if (settings.musicEnabled) (0, audioService_1.startActionMusic)(); else (0, audioService_1.stopMusic)(); return () => (0, audioService_1.stopMusic)(); }, [settings.musicEnabled]);\n'),
    (2299, 2301, "    (0, react_1.useEffect)(() => { const handleKey = (e) => { if (e.key === 'Escape' || e.code === 'KeyP') { e.preventDefault(); setIsPaused(p => !p); } }; window.addEventListener('keydown', handleKey); return () => window.removeEventListener('keydown', handleKey); }, []);\n    (0, react_1.useEffect)(() => { const onVisibility = () => { if (document.visibilityState === 'hidden') setIsPaused(true); }; document.addEventListener('visibilitychange', onVisibility); return () => document.removeEventListener('visibilitychange', onVisibility); }, []);\n"),
    (2335, 2335, '            hudState.comboMultiplier > 1 && react_1.default.createElement("div", { className: "text-yellow-300 font-bold animate-pulse" }, t(\'game_hud_combo\', { multiplier: hudState.comboMultiplier })),\n'),
    (2387, 2389, 'const GameOverScreen = ({ stats, onRestart, onRetry }) => {\n    const { isHighScore, addHighScore, t, settings } = (0, GameContext_1.useGameState)();\n'),
    (2393, 2393, "    const persistPendingScore = (0, react_1.useCallback)(() => {\n        if (isNewHighScore && !scoreSubmitted) {\n            addHighScore(playerName.trim() || 'AAA', stats.score);\n            setScoreSubmitted(true);\n        }\n    }, [isNewHighScore, scoreSubmitted, addHighScore, playerName, stats.score]);\n    const retryNow = (0, react_1.useCallback)(() => { persistPendingScore(); onRetry(); }, [persistPendingScore, onRetry]);\n    const menuNow = (0, react_1.useCallback)(() => { persistPendingScore(); onRestart(); }, [persistPendingScore, onRestart]);\n"),
    (2396, 2401, "        if (!isInputFocused && (event.key === 'r' || event.key === 'R' || event.key === 'Enter')) { event.preventDefault(); retryNow(); }\n        else if (!isInputFocused && event.key === 'Escape') { event.preventDefault(); menuNow(); }\n    }, [retryNow, menuNow]);\n"),
    (2419, 2420, ''),
    (2423, 2424, '    }, [stats, settings.language, isHighScore, t]);\n'),
    (2463, 2464, '        react_1.default.createElement("div", { className: "flex flex-col sm:flex-row gap-3 items-center justify-center" },\n            react_1.default.createElement("button", { onClick: retryNow, className: "font-display text-xl md:text-2xl bg-[var(--primary-bg-tint)] border-2 border-[var(--primary-border)] text-[var(--text-primary)] px-8 py-3 rounded-lg hover:bg-[var(--primary-border)] hover:text-black transition-all shadow-lg shadow-[var(--primary-glow)]/20 transform hover:scale-105" }, t(\'gameover_retry_button\')),\n            react_1.default.createElement("button", { onClick: menuNow, className: "font-display text-lg bg-black/30 border-2 border-gray-500 text-gray-300 px-6 py-3 rounded-lg hover:bg-gray-500 hover:text-white transition-all" }, t(\'gameover_main_menu_button\'))),\n        react_1.default.createElement("p", { className: "text-xs text-gray-400 mt-3" }, t(\'gameover_retry_hint\'))));\n'),
    (2713, 2714, '        react_1.default.createElement("div", { className: "flex flex-col sm:flex-row gap-3 items-center justify-center" },\n            react_1.default.createElement("button", { onClick: retryNow, className: "font-display text-xl md:text-2xl bg-[var(--primary-bg-tint)] border-2 border-[var(--primary-border)] text-[var(--text-primary)] px-8 py-3 rounded-lg hover:bg-[var(--primary-border)] hover:text-black transition-all shadow-lg shadow-[var(--primary-glow)]/20 transform hover:scale-105" }, t(\'gameover_retry_button\')),\n            react_1.default.createElement("button", { onClick: menuNow, className: "font-display text-lg bg-black/30 border-2 border-gray-500 text-gray-300 px-6 py-3 rounded-lg hover:bg-gray-500 hover:text-white transition-all" }, t(\'gameover_main_menu_button\'))),\n        react_1.default.createElement("p", { className: "text-xs text-gray-400 mt-3" }, t(\'gameover_retry_hint\'))));\n'),
    (3251, 3251, 'const UPGRADE_BASE_COSTS = { playerSpeed: 100, bulletSpeed: 150, bulletDamage: 250, maxFuel: 80, maxAmmo: 80 };\n'),
    (3304, 3305, '        const baseCost = UPGRADE_BASE_COSTS[type] || 100;\n'),
    (3312, 3313, '    }, [credits, upgrades]);\n'),
    (3413, 3413, 'let masterGain = null;\nlet compressor = null;\n'),
    (3551, 3553, '        masterGain = audioContext.createGain();\n        compressor = audioContext.createDynamicsCompressor();\n        compressor.threshold.value = -12; compressor.knee.value = 18; compressor.ratio.value = 4; compressor.attack.value = 0.004; compressor.release.value = 0.18;\n        sfxGain.connect(masterGain); musicGain.connect(masterGain); masterGain.connect(compressor); compressor.connect(audioContext.destination);\n        masterGain.gain.value = 0.9;\n'),
    (3600, 3600, "            noise(.09, .20, sfxGain || undefined, 1500); tone(48, .07, .08, 'square', sfxGain || undefined); break;\n"),
    (3601, 3604, "            sfxSweep(420, 980, .18, .14, 'sine'); tone(79, .12, .08, 'triangle', sfxGain || undefined); break;\n        case 'missile':\n            noise(.11, .10, sfxGain || undefined, 650); sfxSweep(220, 760, .24, .13, 'sawtooth'); break;\n        case 'lowFuel':\n            tone(61, .08, .09, 'square', sfxGain || undefined); tone(54, .08, .07, 'square', sfxGain || undefined, .11); break;\n"),
    (4032, 4032, "const detectDefaultLanguage = () => {\n    const raw = (navigator.language || 'en').toLowerCase();\n    if (raw.startsWith('pt')) return 'pt'; if (raw.startsWith('es')) return 'es'; if (raw.startsWith('de')) return 'de';\n    if (raw.startsWith('fr')) return 'fr'; if (raw.startsWith('it')) return 'it'; if (raw.startsWith('ru')) return 'ru';\n    if (raw.startsWith('ar')) return 'ar'; if (raw.startsWith('bn')) return 'bn'; if (raw.startsWith('hi')) return 'hi';\n    if (raw.startsWith('zh')) return 'zh-CN'; if (raw.startsWith('ja')) return 'ja'; if (raw.startsWith('ko')) return 'ko';\n    return 'en';\n};\n"),
    (4047, 4048, '        language: detectDefaultLanguage(),\n'),
