name: Apply fullscreen landscape launcher
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-fullscreen-landscape.yml'
      - '.github/scripts/apply_fullscreen_landscape.py'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Inject fullscreen and landscape runtime
        run: python .github/scripts/apply_fullscreen_landscape.py
      - name: Validate diff
        run: git diff --check
      - name: Commit collection update
        run: |
          git config user.name "Projeto Primata Games"
          git config user.email "guilhermesteiglederdias@gmail.com"
          git add -- '*.html'
          if git diff --cached --quiet; then
            echo "No HTML changes"
            exit 0
          fi
          git commit -m "Add fullscreen landscape launcher to Quickplay"
          git push origin HEAD:main
